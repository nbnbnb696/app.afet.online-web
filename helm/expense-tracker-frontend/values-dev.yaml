namespace: dev

replicaCount: 1

image:
  repository: somanathmuduli03/expensetracker-frontend
  tag: "dev-v.13"
  pullPolicy: Always

service:
  type: ClusterIP

ingress:
  host: app-dev.afet.online
  tlsSecretName: dev-app-afet-tls
